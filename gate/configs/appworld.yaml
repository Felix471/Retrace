# Status: requires gap G2
# G2 must emit records with turn, agent, kind, and content fields.
retrace_mapping: 1
run_discovery:
  pattern: AppWorld/*.txt
  unit: file
run:
  id: "{file_stem}"
event:
  turn: turn
  agent_id: agent
  type:
    from: kind
    map:
      message: message
      tool_call: tool_call
      tool_result: tool_result
      system: system
    default: other
  content: content
  metadata: rest
