This directory is a fully synthetic, deterministic 40-run dataset for trying Retrace's batch, replay, tagging, and compare views. It extends the support-pipeline fixture's run-per-directory event model with three outcomes, three models, three routing variants, varied run lengths, retry loops, and five fixed MAST tag sidecars; regenerate it from the repository root with `python scripts/build_demo.py`.
